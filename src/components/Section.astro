---
interface Props {
    id?: string;
    class?: string;
}

const { id, class: className } = Astro.props;
---

<section id={id} class:list={["section", "reveal", className]}>
    <div class="mx-auto max-w-270 px-6">
        <slot />
    </div>
</section>
