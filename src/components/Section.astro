---
interface Props {
    id?: string;
    class?: string;
}

const { id, class: className } = Astro.props;
---

<section id={id} class:list={["section", "reveal", className]}>
    <div class="container">
        <slot />
    </div>
</section>
